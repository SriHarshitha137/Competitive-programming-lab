#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
#include <climits>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
   int n;
    cin>>n;
    vector<int>vec(n,0);
    for(int i=0;i<n;i++){
        cin>>vec[i];
    }
    int sum=vec[0];
    int maxsum=sum;
    int maxi=INT_MIN;
    for(int j=1;j<n;j++){
        if(vec[j]>=vec[j-1]){
            // maxi=vec[j];
              sum+=vec[j];
             if(maxsum<sum){
              maxsum=sum;
        }      
        }
        else{
            sum=vec[j];
            //   maxi=vec[j];
        if(maxsum<sum){
            maxsum=sum;
        } 
        }
    }
       cout<<maxsum;
    
    
    
    return 0;
}
